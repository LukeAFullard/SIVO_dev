# Modern Glass Template Example

This example demonstrates how to use the `modern_glass.html` dashboard template.
It uses a popular glassmorphism aesthetic with blurred backgrounds, translucent cards, and a clean, modern layout.

## Running the example

```bash
python main.py
```

This will generate an `output.html` file in this directory. Open it in a web browser to see the interactive dashboard.
