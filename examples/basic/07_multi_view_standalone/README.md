# Multi-View Standalone

This example demonstrates how to create a multi-view project, where navigation between different SVG states is bundled together into a single offline HTML app.

Key features shown:
- Setting up multiple distinct interactive `Sivo` maps
- Bundling maps together using `SivoProject(...)`
- Linking interactive elements to other views locally (`drill_to="view_id"`)
