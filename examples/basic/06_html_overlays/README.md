# HTML Overlays

This example demonstrates how to overlay standard HTML elements on top of specific regions or objects in the SVG.

Key features shown:
- Normal mapping (`sivo_app.map(...)`)
- Attaching arbitrary HTML content on top of SVG paths using `sivo_app.add_overlay(...)`
- Positioning adjustments using offsets (`offset_x`, `offset_y`) relative to the mapped element
