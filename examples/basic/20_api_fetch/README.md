# API Fetch

This example demonstrates how to set up elements that trigger data fetching from external endpoints upon interaction, allowing mapping configurations to act as simple API query agents dynamically.

Key features shown:
- Assigning `fetch_url` to an interaction map inside `sivo_app.map(...)`
- Showing returned payloads visually using a positioned interactive panel (e.g. `panel_position="top"`)
