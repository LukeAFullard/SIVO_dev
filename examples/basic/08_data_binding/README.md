# Data Binding

This example demonstrates how to bind external data to elements in your SIVO maps to create color-coded thematic maps.

Key features shown:
- Initializing map elements with default styling
- Binding a dictionary data structure containing numeric values to the map
- The values are mapped to visual gradients via `sivo_app.bind_data(...)` defining custom metrics, colors, and range bounds
