# Drilldowns

This example demonstrates how to set up drill-down navigation in SIVO, where clicking an SVG element causes another SVG to be loaded.

Key features shown:
- Using `drill_to` parameter in `sivo_app.map(...)` to link to a different SVG file
- Interactive visual effects (e.g. `glow`, `hover_color`) giving user feedback
