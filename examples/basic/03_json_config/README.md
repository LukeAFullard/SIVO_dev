# JSON Config

This example demonstrates how to create a SIVO application declaratively by supplying all configuration details, interaction mappings, and element settings through a single `.json` file.

Key features shown:
- Loading configuration from a JSON file using `Sivo.from_config(config_path)`
- Defining mapping and project settings directly in `config.json` instead of running code.
