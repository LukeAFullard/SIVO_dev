# Hover Image Example

This example demonstrates how to use the `hover_image` feature to dynamically change the image fill of an SVG element when hovered.

## Key Features
- `fill_pattern` is used to map an initial image.
- `hover_image` is used to specify a new image URL that appears on mouse hover.
