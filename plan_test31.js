console.log("Wait, if there's a click or mousedown listener on the parent container, which parent container?");
console.log("Maybe `window.addEventListener('mousedown'` or `document.addEventListener('click'`?");
