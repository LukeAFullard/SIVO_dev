1. We need to add a caption to the two SVG maps. `dashboard.add_text_block(block_id="bad_caption", text="...", ...)` below the SVGs. Or we can just use `add_html_block`. Let's add them via `add_text_block` under the svgs in the desktop/mobile grid layout.
2. The user requested: "I'd like the named areas outlines to glow so the user knows to click on them."
Looking at the map signature:
```python
        color: Optional[str] = "transparent",
        hover_color: Optional[str] = None,
        glow: Optional[bool] = None,
```
We can set `glow=True` on the `map()` calls! Or `border_color="rgba(255, 255, 255, 0.8)", glow=True`? Let's just try `glow=True` first on the elements.

Let's modify `examples/mysite/title_page/subpage/air/help/generate_dashboard.py` to:
1. Update `desktop_grid` and `mobile_grid` to include captions.
2. Add the captions.
3. Add `glow=True` to the mapped items.
